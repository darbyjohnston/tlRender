add_subdirectory(tlApp)
add_subdirectory(tlCore)
add_subdirectory(tlIO)
add_subdirectory(tlTimeline)
if(TLRENDER_BUILD_GL)
    add_subdirectory(tlGL)
	if(TLRENDER_BUILD_PROGRAMS)
		add_subdirectory(tlAppBake)
	endif()
endif()
if(TLRENDER_BUILD_QT6 OR TLRENDER_BUILD_QT5)
    add_subdirectory(tlQt)
    add_subdirectory(tlQtQuick)
    add_subdirectory(tlQtWidget)
	if(TLRENDER_BUILD_PROGRAMS)
		add_subdirectory(tlAppPlay)
	endif()
endif()
